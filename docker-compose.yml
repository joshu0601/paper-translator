services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: paperai
      POSTGRES_PASSWORD: paperai
      POSTGRES_DB: paperai
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
